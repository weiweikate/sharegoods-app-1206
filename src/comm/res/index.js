import button from './button';
import placeholder from './placeholder';
import share from './share';
import tab from './tab';

const res = {
    button: {
        ...button
    },
    placeholder:{
        ...placeholder
    },
    share: {
        ...share
    },
    tab: {
        ...tab
    }
};
export default res;
