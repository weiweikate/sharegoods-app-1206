import ShowListPage from "./ShowListPage"
import ShowDetailPage from './ShowDetailPage'

export default {
    moduleName: 'show',
    childRoutes: {
        ShowListPage,
        ShowDetailPage
    }
}
